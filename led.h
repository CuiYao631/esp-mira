#pragma once

// ===========================================================================
// LED 灯效模块：4 路独立 WS2812 灯环 + 动画 + 串口命令处理
// 如需修改灯环引脚/数量、动画算法或命令，编辑 led.cpp 即可。
// ===========================================================================

void ledSetup();                           // 初始化 4 路灯环
void ledTick();                            // 推进动画并按需刷新硬件
void ledProcessCommand(const char* line);  // 处理一条串口命令
void ledPrintCommands();                   // 打印命令菜单
