#pragma once

// ===========================================================================
// 触摸模块：BS8112A 电容触摸芯片（仅使用 KEY 10）
// 如需修改 I2C 引脚、按键编号或状态处理逻辑，编辑 touch.cpp 即可。
// ===========================================================================

void touchSetup();  // 初始化 I2C 与 BS8112A
void touchPoll();   // 轮询触摸并串口打印 PRESS / HOLD / RELEASE
